<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Extensiones</title>
    <%- include('t-dependencias-head') %>
    
</head>
<body>
    <%- include('t-navegador') %>
    <div class="container encabezado mt-5 mb-5">
        <div  style="display: flex; gap: 2rem;">
           
                <h1>Extensiones</h1>

                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalAgregar">
                    <i class="bi bi-plus-circle-fill"></i>
                Nuevo
                </button>           
        </div>     
    </div>
    <hr />
    


<!-- MODAL: AGREGAR NUEVO -->
<div class="modal fade" id="modalAgregar" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Agregar nueva extension</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
            <form method="POST" action="/extension-agregar" id="formulario-agregar">
                
                <div class="form-group">                
                    <label for="nombre">Nombre extension</label>
                    <input name="nombre" class="form-control" type="text" id="formulario-agregar-nombre"><br>

                    <label for="plazo">Plazo en días</label>
                    <input name="plazo" class="form-control" type="number" id="formulario-agregar-plazo"><br>

                    
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                <button class="btn btn-primary" type="submit">Registrar</button>
                </div>
            </form>
        </div>
      </div>
    </div>
</div>


<!--   MODAL EDITAR -->

<div class="modal fade" id="modalEditar" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Editar</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
            <form method="POST" action="/extension-editar" id="formulario-editar">
                <input type="hidden" name="id" value="" id="formulario-editar-id">

                <div class="form-group">                
                <label for="nombre">Nombre extension</label>
                <input name="nombre" class="form-control" type="text" id="formulario-editar-nombre"><br>

                <label for="plazo">Plazo en días</label>
                <input name="plazo" class="form-control" type="number" id="formulario-editar-plazo"><br>
                
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                <button class="btn btn-primary" type="submit">Editar</button>
                </div>
            </form>
        </div>
      </div>
    </div>
</div>



<!-- PRINCIPAL -->
 
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">

            <h3 class="bg-dark text-white px-4 py-2">Listado de extensiones</h3>

            <table class="table">
                <thead>
                  <tr>
                    <th>Id</th>
                    <th>Nomenclador</th>
                    <th>Plazo</th>
                    <th></th>
                  </tr>
                </thead>

                <tbody>
                    <% for (let i = 0; i < extensiones.length; i++) { %>
                        <tr>
                            <%                             
                            var idextension = extensiones[i].id_extension;
                            var nomenclador = extensiones[i].extension;
                            var plazo = extensiones[i].plazo;
                                                      
                            %>

                            <td> <%= idextension %></td>                            
                            <td><%= nomenclador %></td>
                            <td><%= plazo %> días</td>
                            <td>
                                <a href="/extension-borrar/<%= idextension %>">
                                <button type="button" class="btn btn-danger">
                                    <i class="bi bi-trash"></i>
                                </button></a>

                                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalEditar" onclick="formularioeditar('<%= idextension %>', '<%= nomenclador %>', '<%= plazo %>')">
                                    <i class="bi bi-pencil"></i>
                                </button>
                            </td>
                        </tr>
                        <% } %>                  

                </tbody>
            </table>
              
        </div>
    </div>
</div>
      

<!-- SCRIPTS -->
<%- include('t-dependencias-footer') %>

<!-- SCRIPTS -->      
<script>
        var editarFormulario = document.getElementById('formulario-editar');
        var editarCampoId = document.getElementById('formulario-editar-id');
        var editarCampoNombre = document.getElementById('formulario-editar-nombre');
        var editarCampoPlazo = document.getElementById('formulario-editar-plazo');
        
        function formularioeditar(id, nomenclador, plazo){
            editarCampoId.value = id;
            editarCampoNombre.value = nomenclador;  
            editarCampoPlazo.value = plazo;       
            
        }
</script>
    
</body>
</html>